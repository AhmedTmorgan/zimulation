# المساهمة

المشروع مفتوح لصنفين من الناس، ولكلٍّ بابه.

---

## إن كنت فيلسوفًا أو باحثًا: أضِف آلية

لا تحتاج أن تلمس قلب المحاكاة. تكتب ملفًا واحدًا في `mechanisms/`، وتُعلن
فيه أربعة أشياء **قبل** سطر الشفرة الأول:

| | |
|---|---|
| **المصدر** | من قال هذا وأين. لا آلية بلا مرجع — الواجهة ترفضها. |
| **الدعوى** | جملة واحدة **قابلة للتكذيب**: «إن صحّت فسنرى كذا في الأرقام». |
| **المعاملات** | كل ثابتٍ حرّ. تُحصى في `manifest()` ولا تُخفى. |
| **المِعوَل** | تُعطَّل بـ `disable(key)` فتُقارن الدنيا بها وبدونها. |

```python
from village.plugin import mechanism

@mechanism(
    key="my-claim", ar="اسمها بالعربية",
    source="فلان، الكتاب (سنة)، الفصل",
    claim="إن صحّ هذا فسيظهر كذا في السكان بعد كذا جيلًا.",
    params={"gate": 0.6, "strength": 0.2},
    hook="yearly",          # أو on_action / on_birth / on_death / on_wrath
)
def my_mechanism(sim, agent, ctx, rng, P):
    ...
```

ثم أضِف سطرًا في `mechanisms/__init__.py`، واكتب اختبارًا واحدًا على الأقل
في `tests/` يُظهر أن الآلية **تُحدث فرقًا قابلًا للقياس** حين تُفعَّل.

**آليةٌ لا يُرى أثرها في الأرقام لا تُقبل.** إن عطّلتها ولم يتغيّر شيء،
فهي زينةٌ تزيد عدد المعاملات ولا تزيد المعرفة.

---

## إن كنت مطوّرًا: القواعد الثلاث

**١. الحتمية عقدٌ لا يُنقض.**
كل عشوائية تمرّ من `sim.rs.get("اسم")`. لا `random.random()` عارية، ولا
`time`، ولا ترتيب اعتمادًا على `set` أو `dict` غير مرتّب. اختبار
`test_same_seed_same_history` يحرس هذا.

**٢. حاجز الصدق مقدّس.**
`trace.truth` للمراقب وحده. **لا شيء** في مسار قرار الكائن — `mind`,
`speech`, `faith` — يقرؤه. إن احتجت أن تعرف «هل هذا وقع؟» لتقرّر سلوكًا،
فأنت تكتب كائنًا يعرف الغيب. اختبار `test_agent_cannot_read_truth` يحرس هذا.

**٣. النحو لك، والمعنى لهم.**
تكتب التركيب اللغوي؛ الفراغات تُملأ من حياة الكائن الفعلية عبر `speech._*`.
وكل قولٍ يخرج يحمل `why` — نَسَبه. **قولٌ بلا نَسَب هو كذبٌ منّا على القارئ.**
اختبار `test_every_utterance_has_provenance` يحرس هذا.

### قبل أي طلب دمج

```bash
python tests/test_core.py          # كلها تنجح
python study.py --seeds 8 --years 600 --workers 4
```

والثاني ليس للتزيين: تغييرٌ صغير في القلب قد يقلب نصف البذور من نجاةٍ
إلى انقراض. قارِن النِّسَب قبل وبعد، وأرفقها في الطلب.

### أين تضيف ماذا

| | |
|---|---|
| صفة موروثة | `genome.py` — وتذكّر أن قابلية التوريث في `H2` |
| عاطفة | `emotion.py` |
| سلوك | سِمة في `mind.FEATURES` أو فعل في `mind.ACTIONS` + أوزانها في `innate` |
| قانون في العالم | `world.py` — **مخبّأ، بلا أي تلميح** |
| اسمٌ له أصل | `naming.py` |
| ما يقولونه | `speech.py` — نحوٌ وفراغات، لا نصوص |
| دعوى فلسفية | `mechanisms/` |

---

## الحدّ الوحيد

لا نمذجة لممارسةٍ جنسية تشمل أطفالًا. الإكراه بين البالغين يُنمذَج
حدثًا ونتائجَ مجرّدة بلا وصف، كما في `engine._coerce`.

---

## ما نحتاجه أكثر من الميكانيكا

انظر `LIMITS.md`. المشروع لا ينقصه آليات — ينقصه **تحقّق**:

1. حزمة تُشغّل ٥٠ بذرة وتفحص هل تظهر قوانين القوى المعروفة (زيبف للمستوطنات، ريتشاردسون للحروب)
2. تحليل حسّاسية يكشف المعاملات التي لا أثر لها — **لتُحذف**
3. اشتقاق المعاملات الديموغرافية من بيانات منشورة بدل معايرتنا

من أنجز واحدةً من هذه الثلاث خدم المشروع أكثر من عشر آليات جديدة.

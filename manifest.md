# الآليات المسجّلة

معاملات القلب: **67** (حقول `Config`)
مضبوطات مُعلَنة في الوحدات: **46** — `climate` 27، `concept` 12، `faith` 7
آليات مضافة: **2** · معاملاتها الحرّة: **6**

**المجموع المُعلَن: 119 معاملًا حرًّا.** كلّما زاد هذا الرقم قلّت قدرة النموذج على التنبّؤ.

> يُحصى هذا العدد آليًّا من الكود لا يُكتب بيدٍ.
>
> **ووحداتٌ لم تُعلن مضبوطاتها بعد** — وفيها نحو **584** عددًا مضبوطًا غير مُحصًى (تقديرٌ أعلى: كلُّ عددٍ عشريّ مميَّز في مصدرها عدا 0 و1). وهذا نقصٌ يُذكر ولا يُخبّأ، ومعناه أن المجموع الحقيقي أقرب إلى **703** منه إلى 119:
>
> `engine` 81، `causes` 51، `people` 48، `mind` 39، `psyche` 34، `history` 31، `world` 31، `tech` 30، `power` 27، `library` 26، `config` 25، `speech` 25، `emotion` 22، `war` 20، `genome` 16، `memory` 15، `agent` 14، `naming` 12، `plague` 12، `lang` 11، `guard` 6، `observe` 3، `report` 3، `chronicle` 2
> 
> (وبلا أعدادٍ مضبوطة: `plugin`، `rng`)

## إنكار الإرادة (`denial-of-will`) — مفعّلة
- **المصدر:** Schopenhauer, Die Welt als Wille und Vorstellung (1818), الكتاب الرابع
- **الدعوى:** من اجتمع له ألمٌ طويل وتأمّلٌ عالٍ ينصرف عن إشباع الرغبة إلى تعطيلها، فيقلّ نسله واكتنازه ويطول تأمّله — ويدفع ثمنًا تطوّريًا.
- **المِشْبك:** `yearly` · **اشتغلت:** 0 مرة
- **معاملاتها:** `pain_gate=1.05`، `mind_gate=0.62`، `grip=0.34`، `relief=0.45`

## الوسط الذهبي (`golden-mean`) — مفعّلة
- **المصدر:** Aristotle, Ἠθικὰ Νικομάχεια (نحو 340 ق.م)، الكتاب الثاني
- **الدعوى:** طرفا كل صفةٍ مؤذيان لا طرفٌ واحد؛ فمن بعُد عن الوسط في صفاته يعيش أقلّ وينجب أقلّ ممن قارَبه — على الأجيال لا في الجيل.
- **المِشْبك:** `yearly` · **اشتغلت:** 0 مرة
- **معاملاتها:** `cost=0.02`، `reach=0.3`

"""
آلياتٌ مضافة — وهذا هو المكان الذي يُساهَم فيه.

كل ملفٍ هنا يسجّل آلية بمرجعها ودعواها ومعاملاتها. القلب لا يعرف بها،
ويمكن تعطيلها كلها فيعمل العالم بدونها — وهذا شرطُ أن تكون قابلة للاختبار:
إن لم تستطع أن تُطفئها فلا تستطيع أن تعرف ماذا فعلت.

    import mechanisms          # تُسجَّل كلها
    from village import plugin
    print(plugin.manifest(plugin.count_core_params(cfg)))
"""

from . import schopenhauer_denial      # noqa: F401
from . import aristotle_mean           # noqa: F401
